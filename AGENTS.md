# 项目规则

- 每次对项目文件做出修改后，必须使用 Git 提交。
- Git 提交信息必须使用中文，并简要说明本次修改内容。
- 每次 Git 提交完成后，必须自动推送到已关联的远端仓库。
- 以后所有项目修改都必须直接在主干分支 `master` 上完成，不新建或切换到功能分支。
- 如果本地正在运行的工具窗口锁住默认构建输出目录，必须强行关闭该工具进程后，再使用默认输出目录进行构建、测试或启动验证。
